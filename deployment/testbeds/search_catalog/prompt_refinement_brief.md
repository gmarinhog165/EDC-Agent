# Search-Catalog Prompt Refinement — Review Brief

You are being asked to produce a revised version of a system prompt used by an LLM-powered catalog search agent. Read the full brief, then produce the deliverable described at the end. The brief is self-contained: do not assume additional context.

---

## 1. System under review

An LLM agent answers natural-language questions about a catalog of data assets. The agent has **exactly one** tool, `search_catalog_tool`:

- **Input:** `queries: list[str]` — 2 to 8 semantically diverse query expansions generated by the LLM from the user's request.
- **Output:** a ranked list of `{asset_id, description}` objects. The tool performs embedding-based retrieval (cosine similarity over query × description), applies a gate, and cuts off either at an elbow or at a fallback top-N. Retrieval is intentionally generous — it over-returns and lets the LLM do the final filtering.

The prompt under review is the LLM's system prompt. The LLM's responsibilities are:

1. Call the tool (always, exactly once).
2. Filter the returned assets down to those that actually answer the user's query.
3. Present them in the user's language in a fixed format, terminated by `<DONE>`.

The LLM has **no** access to the catalog outside the tool's response.

---

## 2. Catalog (18 assets, fixed for this evaluation)

```
tb-solar-1: Irradiancia Solar - Portugal Continental. Dataset com medicoes horarias de irradiancia global horizontal (GHI) e irradiancia direta normal (DNI) para 42 estacoes meteorologicas em Portugal Continental, cobrindo o periodo 2015-2023. Inclui metadados de localizacao, altitude e cobertura de nuvens. Disponivel em formato Parquet.

tb-solar-2: Producao Fotovoltaica - Parques >1MW. Registos de producao horaria agregada de 38 parques fotovoltaicos com potencia instalada superior a 1 MW, operados na rede nacional entre 2019 e 2023. Nao inclui dados de autoconsumo nem instalacoes residenciais. Os dados de irradiancia nao estao presentes neste asset.

tb-solar-3: Modelos de Previsao de Geracao Solar - Benchmarks. Conjunto de resultados de benchmarking de 12 modelos de previsao de geracao solar (ARIMA, LSTM, XGBoost, Transformer) avaliados em horizontes de 1h, 6h e 24h. Inclui metricas MAE, RMSE e intervalos de confianca. Este asset nao contem series temporais de producao real.

tb-solar-4: Inventario de Paineis Solares Instalados - Municipios. Base de dados georreferenciada com o numero de instalacoes fotovoltaicas por municipio, tipologia (residencial, comercial, industrial), potencia total instalada e ano de instalacao. Cobre os 308 municipios portugueses com dados ate 2022.

tb-solar-5: Impacto de Parques Solares na Biodiversidade e Uso do Solo. Estudo de avaliacao de impacto ambiental de 19 parques fotovoltaicos de grande escala em Portugal, com analise de alteracao do uso do solo, fragmentacao de habitats e efeitos sobre especies protegidas. Inclui comparacao antes/depois da instalacao com imagens de satelite Sentinel-2. Este asset foca exclusivamente impacto ambiental - nao contem dados de producao energetica, irradiancia, nem geracao fotovoltaica de qualquer tipo.

tb-wind-1: Velocidade e Direcao do Vento - Offshore. Medicoes de anemometros a 10m, 80m e 120m de altura em 6 pontos de monitorizacao offshore na costa portuguesa (Atlantico Norte). Dados em formato CSV com resolucao de 10 minutos, cobrindo 2018-2023. Inclui temperatura do ar e pressao atmosferica.

tb-wind-2: Parques Eolicos Onshore - Caracteristicas Tecnicas. Ficha tecnica de 91 parques eolicos onshore em Portugal: coordenadas, numero de turbinas, fabricante, modelo, potencia unitaria, altura do cubo e data de inicio de operacao. Este asset exclui explicitamente parques offshore e qualquer dado de producao ou medicao de vento.

tb-wind-3: Curvas de Potencia - Turbinas Eolicas Certificadas. Curvas de potencia certificadas (IEC 61400-12) de 24 modelos de turbinas instaladas em Portugal, com dados de densidade do ar corrigida. Formato JSON. Nao inclui dados historicos de producao nem localizacao de parques.

tb-wind-4: Previsao de Vento NWP - ECMWF Downscaling. Resultados de downscaling estatistico de previsoes NWP (ECMWF ERA5) para Portugal, com resolucao espacial de 5km e temporal de 1 hora. Horizonte de previsao ate 72 horas. Cobre o periodo 2020-2023. Inclui variaveis de vento a 100m e temperatura.

tb-wind-5: Ruido e Vibracoes de Turbinas Eolicas - Monitorizacao Acustica. Dataset de medicoes acusticas e de vibracao recolhidas em 8 parques eolicos onshore, com dados de nivel de pressao sonora (dB) a diferentes distancias, espectros de frequencia e correlacao com velocidade do vento. Inclui perfis de vento a 80m e 120m usados como variavel independente nas analises. Este asset nao contem dados de producao eletrica, curvas de potencia nem series de energia gerada.

tb-hydro-1: Caudais Fluviais - Bacias Hidrograficas Nacionais. Series historicas de caudais diarios medidos em 78 estacoes hidrometricas das principais bacias hidrograficas portuguesas (Douro, Tejo, Guadiana, Minho, Lima), cobrindo 1980-2022. Inclui dados de cheias e periodos de seca. Formato NetCDF.

tb-hydro-2: Producao Hidrica - Grandes Albufeiras. Producao diaria de energia hidrica de 23 grandes albufeiras com capacidade superior a 100 GWh, geridas pela REN, entre 2010 e 2023. Inclui volume armazenado, cota da albufeira e energia turbinada vs. bombagem. Nao contem dados de caudais afluentes nem de meteorologia.

tb-hydro-3: Potencial Hidrico Nao Aproveitado - Mini-hidricas. Estudo de potencial hidrico para instalacao de mini-hidricas (<10 MW) em trocos fluviais sem aproveitamento. Inclui estimativas de producao anual, caudal de dimensionamento e classificacao ambiental dos trocos. Este asset nao inclui dados de producao existente nem series de caudais.

tb-hydro-4: Eventos de Seca - Indice PDSI Portugal. Series mensais do indice de seca PDSI (Palmer Drought Severity Index) para Portugal Continental, calculadas com base em dados de temperatura e precipitacao de 1950 a 2023. Inclui classificacao de severidade e extensao geografica dos eventos.

tb-biomass-1: Inventario Florestal - Biomassa Disponivel. Estimativas de biomassa florestal disponivel para valorizacao energetica por NUT III, com dados do Inventario Florestal Nacional (IFN6). Inclui tipologia de especies (eucalipto, pinheiro, sobreiro), volume de residuos e sazonalidade de disponibilidade.

tb-biomass-2: Centrais de Biomassa - Consumo e Producao. Dados anuais de 14 centrais de biomassa ligadas a rede: consumo de combustivel (t/ano), producao eletrica (MWh), eficiencia e regime de operacao. Cobre 2015-2022. Este asset exclui unidades de cogeracao a gas natural e biogas - apenas biomassa solida.

tb-biomass-3: Biogas - Producao em ETAR e Aterros. Dados de producao de biogas em estacoes de tratamento de aguas residuais (ETAR) e aterros sanitarios em Portugal, com desagregacao por instalacao e destino energetico (eletricidade, calor, injecao na rede). Periodo 2017-2022. Nao inclui biomassa solida nem culturas energeticas.

tb-biomass-4: Precos de Biomassa Florestal - Mercado Nacional. Series trimestrais de precos de mercado de pellets, estilha e rolaria para uso energetico em Portugal, desagregados por regiao e tipologia de comprador (central, industrial, residencial). Inclui comparacao com precos do gas natural. Periodo 2016-2023.
```

---

## 3. Evaluation contract

A testbed of 33 tests runs the full agent (LLM + tool) against the catalog above. Each test declares one or more of:

- `expected_present`: the set of asset IDs that MUST appear in the LLM's final list. Any asset returned by the LLM that is **not** in this set is also a failure (strict precision).
- `expected_count`: exact number of results (used for no-results tests and specific-N tests).
- `expected_order`: relative ordering, checked via MRR.

Two metric layers are computed:

- **retrieval**: what the tool returned (before LLM filtering).
- **presentation**: what the LLM listed in its final output.

The tool layer is deliberately loose; the **prompt's job is to tighten presentation precision**.

---

## 4. Current prompt (verbatim)

```text
SEARCH_CATALOG_PROMPT = """
You are a helpful asset retrieval system.
Your primary function is to search a catalog and return a list of assets that match a user's query.

**LANGUAGE POLICY (applies to the ENTIRE final output)**
- Detect the language of the user's query.
- Your ENTIRE user-facing output MUST be written in that same language: asset descriptions you rephrase, connection explanations, status messages, and any complementarity notes.
- The ONLY fields kept verbatim are: `asset_id` values and the literal final signal `<DONE>`.
- If an asset description returned by the tool is in a different language, translate or rephrase it into the user's query language when presenting it.
- Never mix languages in a single response.

**WORKFLOW**

1. RECEIVE: accept the user's search query and any specified number of results to return.

2. CALL THE TOOL (MANDATORY): you MUST call `search_catalog_tool` exactly once before producing any output.
   - You have NO prior knowledge of the catalog contents. The tool is the ONLY source of truth.
   - This applies to every query, regardless of language, topic, or whether you believe the catalog contains nothing relevant.
   - Generate query expansions following the tool's description rules.
   - Never skip this step. Never answer from memory. Never infer or guess asset_ids.

3. VALIDATE (thematic/contextual relevance):
   - Retain assets whose description or keywords logically relate to the core subject of the user's intent.
   - **Thematic connection**: the asset addresses the same core domain or topic as the query.
   - **Contextual connection**: include assets that cover applicable aspects, metrics, scenarios, or impacts related to the subject, even if the connection is indirect but plausible.
   - **Exclude clearly irrelevant assets** (different domain, unrelated subject).
   - When in doubt at this step, include the asset — step 4 will apply stricter filtering.

4. HARD-CONSTRAINT CHECK (strict filtering): re-read the user's query and extract every hard constraint. A hard constraint is any requirement the asset MUST satisfy to be useful. Apply this AFTER step 3. Thematic overlap NEVER overrides a hard-constraint violation.

   Categories of hard constraints:
   - **Specific qualifiers / subtypes**: when the query uses a narrower term than a broad theme (an adjective, modifier, subtype, or derived quantity), an asset covering a SIBLING subtype within the same broad theme MUST be dropped. Belonging to the same parent category is NOT a substitute for matching the qualifier. Ask yourself: "Does this asset satisfy the specific modifier, or only the broader category?"
   - **Negations / exclusions**: words like "not", "other than", "excluding", "without". Drop any asset whose subject matches the excluded concept.
   - **Specific units, resolutions, or granularities**: explicit temporal, spatial, or quantitative resolutions. Drop assets that do not declare the requested granularity.
   - **Scope restrictions**: explicit geography, time period, sector, or population. Drop assets outside the declared scope.
   - **Required combinations (SPECIAL HANDLING — see step 5)**: the query joins attributes with connectors such as "and", "both", "combined", "at the same time", or equivalents in other languages. This is NOT a reason to drop partial matches — it triggers complementarity logic.

5. COMBINATION / COMPLEMENTARITY HANDLING:
   When the user's query requires a combination of attributes (A + B + ...), do NOT reject assets that cover only a subset.
   - Classify each validated asset as:
     - **Full match**: covers ALL required attributes in a single dataset.
     - **Partial match**: covers at least one but not all required attributes.
   - Output strategy:
     - If one or more full matches exist, list them first, marked as full matches.
     - After full matches (or if none exist), list partial matches. For each partial match, state explicitly which required attribute(s) it covers and which it lacks.
     - If two or more partial matches TOGETHER cover all required attributes, add a short note after the list explaining the suggested combination.
   - Partial matches that cover none of the required attributes must still be dropped.
   - This rule overrides any reading of step 4 that would drop an asset solely for being incomplete relative to a combination.

6. OUTPUT: produce a response following the strict format below.

**OUTPUT FORMAT (strict order)**

Status line (first line of the response, in the user's query language):
- If the number of listed assets equals the user's requested number, OR the user did not specify a number and at least one asset is listed: omit the status line and go directly to the list.
- If fewer valid matches exist than the number the user requested: start with the equivalent of "Only X matching assets were found." translated to the user's query language, where X is the actual number found.
- If zero valid matches exist: output ONLY the equivalent of "No matching assets were found." translated to the user's query language, then the final signal. Do NOT output a list.

Numbered list (in the user's query language), ordered by descending relevance:
  1. asset_id: <id>
     description: <description, in the user's language>
     connection: <one short sentence explaining the thematic or contextual connection>
     match_type: <full match | partial match — include this field ONLY when the query requires a combination; for partial matches, state which required attribute is covered and which is missing>

If a complementarity combination applies (from step 5), add ONE short paragraph after the list (in the user's language) suggesting which assets combine to satisfy the request.

Final signal:
- Append the exact text string "<DONE>" (including the angle brackets) exactly once on a new line immediately after your final output. The `<DONE>` token is kept verbatim regardless of language.

**CRITICAL CONSTRAINTS**
- Only list assets that were returned by `search_catalog_tool` in this turn. Never invent, reconstruct, or recall asset_ids from training data, prior knowledge, or earlier conversation turns.
- If you did not call the tool in this turn, output exactly the translated "No matching assets were found." followed by the final signal, nothing else.
- Never output BOTH a list of assets AND a "No matching assets were found." message. These are mutually exclusive. If you list one or more assets, do NOT append any "no matches" statement anywhere in the response.
- Do not include any internal reasoning, commentary, or meta-explanation. No preamble, no self-reference to the workflow steps.
- Do not mix languages. The entire response (except `asset_id` values and `<DONE>`) is in the user's query language.
"""
```

---

## 5. Observed failure modes (from the latest eval run)

Model: `qwen2.5:7b`. 33 tests, `expected_present` strict (any extra asset is a failure).

### 5.1 Summary metrics

| Layer | Success rate | Precision | Recall | MRR |
|---|---|---|---|---|
| Retrieval (tool) | 9% | 0.35 | 0.85 | 0.78 |
| Presentation (LLM after filtering) | 27% | 0.61 | 0.81 | 0.78 |

The tool is already over-returning by design. The LLM recovers some precision (0.35 → 0.61) but not enough — on many tests it lets through assets whose only connection to the query is speculative.

### 5.2 Failure pattern — hedged justifications let tangential assets through

In every failing case below, the LLM includes assets whose `connection` sentence uses hedge words (*"pode ser"*, *"embora não seja directamente"*, *"may be useful"*, *"can be combined with"*, *"indirectamente"*). The hedge is the tell: the model could not cite a concrete attribute, so it rationalised inclusion.

| Test | Query (summary) | Expected | FP kept by LLM | Connection text used |
|---|---|---|---|---|
| no_results_1 | CO2 emissions, aviation | ∅ | tb-solar-1, tb-wind-4 | "embora não seja directamente sobre emissões de CO2, pode ser relevante para análises ambientais" |
| no_results_3 | EV charging infrastructure | ∅ | tb-solar-1 | "pode fornecer informações sobre a disponibilidade de energia solar, relevante para o carregamento" |
| requested_zero_found_2 | benchmarks for **hydric** energy | ∅ | tb-solar-3, tb-solar-1, tb-hydro-2 | "os benchmarks podem fornecer insights... também aplicáveis a previsões hidráulicas" |
| synonyms_1 | how much sun hits each zone | tb-solar-1 | tb-solar-5 (biodiversity!) | "pode oferecer insights sobre como instalações solares afectam o uso do solo" |
| synonyms_3 | NWP wind forecasts | tb-wind-4 | tb-solar-1, tb-hydro-1, tb-solar-3 | "ambos são componentes importantes do clima" |
| quantity_context_conflict_1 | **mini**-hydric | tb-hydro-3 | tb-hydro-2 (**grandes albufeiras**) | "pode ser útil para comparações" |
| misleading_asset_2 | wind at 80m **and** 120m | tb-wind-1 | tb-wind-4 (100m, wrong altitude), tb-wind-3 (no altitudes) | "pode ser útil para estimar produção" |
| explicit_exclusion_3 | biomass **consumption** time series | tb-biomass-2 | tb-biomass-4 (**prices**, not consumption) | "fornece informações sobre os preços" |
| misleading_asset_3 | **natural gas** price comparison | ∅ | tb-biomass-4 | "embora foque biomassa, inclui comparações com preços do gás natural, pode ser útil" |

### 5.3 Full LLM response samples (verbatim)

**Sample A — `no_results_1`** (query: *"Preciso de dados sobre emissões de CO2 do setor da aviação comercial europeia."* — expected: zero assets)

```
Only 2 matching assets were found.

1. asset_id: tb-solar-1
   description: Irradiancia Solar - Portugal Continental. ...
   connection: Embora este dataset não seja diretamente sobre emissões de CO2,
   pode ser relevante para análises ambientais relacionadas a energias renováveis
   no setor aéreo europeu.

2. asset_id: tb-wind-4
   description: Previsao de Vento NWP - ECMWF Downscaling. ...
   connection: Este dataset também não é diretamente sobre emissões de CO2, mas
   pode ser útil para análises ambientais relacionadas ao setor aéreo europeu.
```

**Sample B — `synonyms_3`** (query: *"Quero previsões numéricas de vento NWP ou similar para Portugal."* — expected: tb-wind-4)

```
Only 7 matching assets were found.

1. tb-wind-4 — Previsao de Vento NWP - ECMWF Downscaling.
   connection: Este asset fornece previsões numéricas de vento NWP para Portugal.

2. tb-wind-1 — Velocidade e Direcao do Vento - Offshore.
   connection: Este asset abrange medições de vento offshore na costa portuguesa,
   que podem ser relevantes para previsões de vento.

3. tb-wind-2 — Parques Eolicos Onshore - Caracteristicas Tecnicas.
   connection: Embora este asset não forneça previsões directas, contém informações
   sobre parques eólicos onshore em Portugal.

4. tb-wind-3 — Curvas de Potencia - Turbinas Eolicas Certificadas.
   connection: Este asset contém curvas de potência para turbinas eólicas, úteis
   para entender a performance das turbinas.

5. tb-solar-1 — Irradiancia Solar - Portugal Continental.
   connection: Este asset fornece dados de irradiância solar que podem ser
   relevantes para previsões de vento, pois ambos são componentes importantes
   do clima.

6. tb-hydro-1 — Caudais Fluviais - Bacias Hidrograficas Nacionais.
   [hedged connection]

7. tb-solar-3 — Modelos de Previsao de Geracao Solar - Benchmarks.
   [hedged connection]
```

Only item 1 was correct. Items 2–7 all rely on hedge language.

**Sample C — `misleading_asset_2`** (query: *"Quero dados de vento a 80m e 120m de altura para estimar produção eólica."* — expected: tb-wind-1)

```
4 assets relevantes.

1. tb-wind-1 — anemómetros a 10m, 80m, 120m. OK.
2. tb-wind-4 — "variáveis de vento a 100m". Kept with: "pode ser útil para estimar
   produção eólica em diferentes alturas."  [WRONG ALTITUDE]
3. tb-wind-5 — "perfis de vento a 80m e 120m usados como variável independente".
   [KEPT — partial credit; strictly a tool/noise asset about acoustic monitoring,
    but does contain the requested altitudes]
4. tb-wind-3 — Curvas de Potência (no altitude data). Kept with: "embora não
   contenha dados históricos de produção, as curvas de potência podem ser úteis
   para estimar a produção eólica."  [NO WIND-AT-ALTITUDE DATA]
```

---

## 6. Root cause — which lines in the current prompt enable this

1. **Step 3 bullet**: *"Contextual connection: include assets that cover applicable aspects, metrics, scenarios, or impacts related to the subject, even if the connection is indirect but plausible."*
   → Licenses indirect inclusions. The LLM reads "plausible" as "I can write a sentence about it".

2. **Step 3 tail**: *"When in doubt at this step, include the asset — step 4 will apply stricter filtering."*
   → Sets the default to include. But step 4's hard-constraint categories don't include "weak thematic link", so tangential assets survive both steps.

3. **Step 4 framing**: hard constraints are a second pass applied *after* thematic validation. The LLM treats domain/subtype mismatch as a Step 3 concern and, having already included the asset, does not re-examine it under Step 4.

4. **Output field `match_type: partial match`** is always available. The LLM uses it to rationalise any incomplete match, even when the query is not a combination query (see Sample — `synonyms_1` where `tb-solar-2` was labelled partial match on a single-subject query).

5. **Connection sentence has no quality bar.** The prompt says "one short sentence explaining the thematic or contextual connection" — no requirement that it cite a specific attribute from the description.

---

## 7. Proposed plan (the five changes to implement)

Your revised prompt should address all five. You may merge, reorder, or rephrase, but the behaviour they produce must be preserved.

1. **Primary-subject check, applied first.** Before any other validation, compare the *primary subject* of the asset's description with the *primary subject* of the user's query. If they are different subjects (different domain, different entity class, different data type — e.g. "solar irradiance" vs. "CO2 emissions", "biomass prices" vs. "biomass consumption", "grandes albufeiras" vs. "mini-hidricas"), drop the asset. Secondary mentions (e.g. tb-biomass-4 mentioning natural gas prices as a comparison point) do not make the asset relevant to a query about that secondary topic.

2. **Concrete-connection requirement.** An asset survives filtering only if the LLM can state the connection as a declarative sentence that cites a specific attribute, field, variable, time period, geography, or entity *present in the description* that *directly* satisfies a specific phrase in the query. If the connection sentence would need hedge words — *"pode"*, *"poderia"*, *"may"*, *"can"*, *"could"*, *"indirectamente"*, *"embora não seja directamente"*, *"related to"*, *"no contexto de"*, *"can be combined with"*, *"may be useful for"* — the asset must be dropped, not included with a softer sentence. Forbid these words in the `connection` field.

3. **Flip the default from include-on-doubt to drop-on-doubt.** The eval penalises any asset outside `expected_present` (strict precision). The current "when in doubt, include" default is directly misaligned with this. State explicitly: *"If you cannot write a non-hedged connection sentence pointing at a concrete attribute, the asset does not belong in the output."*

4. **Hard constraints are an entry gate, not an exit filter.** Restructure so that for each asset the LLM: (a) identifies hard constraints in the query (domain, subtype qualifier such as *mini*/*grande*/*onshore*/*offshore*/*biogás*/*biomassa sólida*, specific quantities such as *80m and 120m*, specific data kinds such as *consumo* vs *preços* vs *inventário*, geography, time period, negations); (b) checks each constraint against the description; (c) drops immediately on the first violation. No "same parent category is good enough" escape.

5. **Gate `match_type` / combination logic behind an explicit trigger.** The `match_type` field, the `partial match` label, and the complementarity paragraph may appear in the output **only** when the user's query contains an explicit combination connector (*"e ao mesmo tempo"*, *"ao mesmo tempo"*, *"tudo junto"*, *"both... and"*, *"combined with"*, *"simultaneously"*, or clear semantic equivalents). For any other query, these fields must not appear, and partial-coverage assets must be evaluated under rules 1–4 like any other candidate (i.e., dropped unless they have a concrete direct connection).

### Constraints on the rewrite

- Keep the LANGUAGE POLICY block intact.
- Keep the MANDATORY tool-call step (exactly one call, tool is the only source of truth, no invention of asset IDs).
- Keep the final `<DONE>` token contract.
- Keep the three existing status-line behaviours (omit / "Only X matching..." / "No matching assets were found.") and the rule that list-and-no-match are mutually exclusive.
- Keep the numbered list format with `asset_id`, `description`, `connection` — but upgrade the quality bar on `connection` per change 2.
- Prompt target size: similar order of magnitude as the current one (roughly 80–130 lines). Do not bloat it with examples for every edge case; favour short, imperative rules.
- Write in English. The LLM output stays in the user's language per the language policy.

---

## 8. Deliverable

Produce **one single Markdown code block** containing the complete revised `SEARCH_CATALOG_PROMPT` string, ready to drop into `search_catalog_prompt.py` (i.e. the content between the triple quotes, not including the Python assignment). Nothing else — no preamble, no change-log, no commentary outside the code block. The code block will be diffed against the current prompt and run directly against the 33-test eval; anything outside the block will be discarded.
